# parentRefs are set at deploy time from feature config (gatewayName / gatewayNamespace)
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: provider-name
  namespace: agentgateway-system
  labels:
    app.kubernetes.io/managed-by: agentic-demo
    agentgateway.dev/provider: provider-name
spec:
  parentRefs:
    - name: agentgateway
      namespace: agentgateway-system
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /provider-name
      backendRefs:
        - name: provider-name
          namespace: agentgateway-system
          group: agentgateway.dev
          kind: AgentgatewayBackend
